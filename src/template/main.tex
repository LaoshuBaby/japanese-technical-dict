% !TEX TS-program = xelatex
% !TEX encoding = UTF-8 Unicode

% \documentclass{article}

\documentclass[10pt]{article} % 设置默认字号为10pt

\usepackage[utf8]{inputenc} % 输入编码
\usepackage[T1]{fontenc} % 字体编码
\usepackage{geometry} % 页面布局
\usepackage{multicol} % 多栏布局
\usepackage{booktabs} % 表格横线

\usepackage{xeCJK}

\usepackage{longtable} % 引入 longtable 宏包
\usepackage{booktabs} % 用于更漂亮的表格线
\setCJKmainfont{SimSun} % 将“SimSun”替换成您电脑上的中文字体


% 设定页面大小，你可以根据自己的需要调整
\geometry{
  paperwidth=7cm,
  paperheight=10cm, % 这里是打开后的尺寸，所以是5cm的两倍
  left=0.5cm,
  right=0.5cm,
  top=0.5cm,
  bottom=1cm
}

% 设置multicol环境的分隔线没有距离
\setlength{\columnsep}{0pt}

\begin{document}
% \begin{multicols}{2} % 开始双栏

    \begin{longtable}{@{}p{\dimexpr0.33\linewidth-2\tabcolsep}%
                       p{\dimexpr0.33\linewidth-2\tabcolsep}%
                       p{\dimexpr0.33\linewidth-2\tabcolsep}@{}}
    
    \toprule % 表格顶部的横线
    EN & ZH & JA \\ \midrule
    \endhead % 表格头部定义结束
    
    \midrule
    \endfoot % 表格脚注定义结束
    
    \bottomrule
    \endlastfoot % 表格最后一页的脚注定义结束
    
    % 词汇对照的具体内容将位于此
    % ...
    Word & 单词 & タンゴ \\ \midrule
    
    \end{longtable}
    

% 你可以复制上面的表格块添加更多的词汇

% \end{multicols}
\pagenumbering{arabic} % 页码使用阿拉伯数字
\end{document}
